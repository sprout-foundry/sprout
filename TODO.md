# TODO

Active work tracked here. Completed items are removed once their parent spec is
done — the spec file (`roadmap/SP-###.md`) plus git history are the historical
record.

**Status of related specs:** SP-063 (`computer_user` persona) is **partially implemented** — its core shipped; remaining work (panic key 4g, destructive-app denylist 4h) is tracked in `roadmap/SP-063-computer-use-persona.md`, not here. SP-073 (`cooperative cancellation`) shipped 2026-06-17; further work would be new tickets, not this list.

## SP-068: Security Check Consolidation — One Risk Scale, One Resolver, One Broker
_Spec: `roadmap/SP-068-security-check-consolidation.md`_

Refactor (Medium): the same `shell_command` is judged by two independent risk taxonomies in sequence plus four special-case gates; three separate mechanisms (`IsShellCommandAllowlisted`, `HasUserApproval`, `consumeShellCommandApproval`) exist almost entirely to suppress the second prompt. No new user-facing capability — closes the "why was this gated?" audit gap and removes the double-prompt regression class.

- [x] SP-068-1: **Phase 1 — One risk scale (non-behavioral mapping).** Add `RiskAssessment` to `pkg/agent_tools/risk_resolver.go` carrying canonical level (`Low/Medium/High/Critical`), contributing sources (classifier, persona cascade, git-history-rewrite, fs-tier, workspace policy), a human reason string, and an `IsHardBlock` flag. Map `SAFE→Low`, `CAUTION→Medium`, `DANGEROUS→High`, critical-op hard-block→`Critical`. Add `risk_resolver_test.go` with golden-test corpus asserting byte-identical gating for every existing classifier call site. Acceptance: `make build-all` passes; `go test ./pkg/agent_tools/...` green; the corpus confirms zero decision changes. _Verified shipped at `pkg/agent/risk_assessment.go` (file location intentional — `ResolveToolRisk` is an `*Agent` method). Build + targeted tests green (32/32)._
- [x] SP-068-2: **Phase 2 — One resolver.** Implement `ResolveToolRisk(toolName, args, agent) RiskAssessment` in `pkg/agent_tools/risk_resolver.go` taking the most restrictive input (Critical → built-in DANGEROUS / git-history-rewrite / workspace-deny → persona cascade (High) → user-DANGEROUS patterns → Caution (Medium) → Safe (Low)). Gate behind `unified_risk_resolver` config flag (default off for one release). Add shadow-mode logging comparing old-vs-new decisions on every call. Acceptance: in shadow mode over a 100-command test corpus, the only decisions that differ are de-duplicated prompts; no decision changes for any critical-op hard-block. _Verified shipped at `pkg/agent/risk_assessment.go` (`ResolveToolRisk`, `unified_risk_resolver` flag, shadow-mode logging in seed_tool_registry.go + tool_handlers_shell.go). Corpus grown from 6 → 113 commands in `TestShadowMode_ParityFullCorpus` covering safe-reads, git-reads, caution, git-write, dangerous, and critical-ops categories. Build + all targeted tests green._
- [x] SP-068-3: **Phase 2 follow-up — flip the default + delete the suppression plumbing.** Turn `unified_risk_resolver` on by default after one release with no drift detected; delete `HasUserApproval`, `consumeShellCommandApproval`, and the per-agent seed pre-execute approval map in `pkg/agent/risk_prompt.go` and `pkg/agent/seed_tool_registry.go`. Acceptance: `grep -rn "consumeShellCommandApproval\|HasUserApproval" pkg/` returns nothing except the deletion commit; regression test asserts exactly one approval request per gated call (no double prompts); `rm -rf /` and the fork-bomb corpus still hard-block under the unified resolver. _Verified shipped: Go zero-value default is false (TestConfigUnifiedRiskResolver_DefaultFalse), applyUnifiedRiskResolverDefault flips it on for migrated users. Suppression plumbing deleted (no `func` references remain — only 3 explanatory comments noting the removal). Single-approval regression test at sp068_regression_test.go:40 (TestSP068_UnifiedGateApprovesMediumCommand). Critical hard-block tests at sp068_regression_test.go:158 cover rm -rf /, mkfs, dd, fork bombs with allowlist attempts._
- [x] SP-068-4: **Phase 3 — One broker + `sprout explain`.** Add `ApprovalBroker.Request(assessment) Decision` in `pkg/agent/approval_broker.go` owning surface selection (webui vs CLI), timeout, fallback, and 4-option outcome; collapse both Gate 1 and Gate 2 call sites in `pkg/agent/tool_security.go` and `pkg/agent/risk_prompt.go` to a single broker call. Add `cmd/explain.go` — `sprout explain '<command>'` (and `--why` on the security error) printing the full `RiskAssessment`: canonical level, every contributing source, and the exact rule that set the level. Acceptance: `sprout explain 'git reset --hard HEAD~5'` prints `High/Critical` with the contributing source annotated; `make build-all` passes; one regression test asserts a single `approval_requested` event per gated call. _Verified shipped: `pkg/agent/approval_broker.go` exposes `RequestApproval(assessment, toolName, args)` returning `BrokerDecision`. Both Gate 1 (`tool_security.go` legacy path) and Gate 2 (`risk_prompt.go` `highRiskApprovedForCommand`) collapse through it. `cmd/explain.go` (existed) renders full `RiskAssessment`. `--why` flag added in `cmd/root.go` + rendered in `cmd/cli_error.go` when `AgentError.Category == CategorySecurity`. SecurityError carries the assessment via `pkg/errors.NewSecurityErrorWithAssessment`. Four regression tests in `approval_broker_test.go` (`TestSP068_SingleApprovalEvent_{WebUI,NonInteractive,LowRisk,UnifiedGate}`) assert exactly one `security_approval_request` event per gated call across all three surfaces. Build + all targeted tests green._

## SP-062: CLI-Native Background Shell Execution
_Spec: `roadmap/SP-062-cli-background-shell.md`_

High priority (~1 week, 4 phases). The LLM is told about `background`/`check_background`/`stop_background` parameters that silently fail in pure-CLI mode (no WebUI, `--no-web-ui`, non-interactive `sprout agent "..."`). Long-running commands hit the 2-minute timeout and are killed with no recovery path.

- [ ] SP-062-1: **Phase 1 — `BackgroundProcessManager` core.** Create `pkg/agent_tools/background_process.go` with `BackgroundProcess` (`ID`, `Cmd`, `OutputFile`, `Dir`, `StartedAt`, `LastPolled`, `done`, `mu`) and `BackgroundProcessManager` (`processes map`, `mu`, `expiry`, default 2h). Methods: `Start(ctx, command, dir)`, `CheckOutput(sessionID)`, `Stop(sessionID)`, `IsActive(sessionID)`, `Adopt(cmd, outputFile, dir)` (for timeout promotion), and a `cleanupLoop()` goroutine (60s tick, reaps exited + inactive processes, deletes temp files after 5min). Session ID format `bg-<prefix>-<8-hex>` matching WebUI; output to `~/.config/sprout/background/<sessionID>.output` via `io.MultiWriter`. Add `background_process_test.go` covering Start/Check/Stop/Adopt, exit detection, concurrent cap of 5. Acceptance: `go test ./pkg/agent_tools/...` green; the cleanup goroutine reaps inactive processes after expiry.
- [ ] SP-062-2: **Phase 2 — Timeout promotion in CLI path.** Restructure `runShellCommand` in `pkg/agent_tools/shell_native.go` to always pipe output to the temp file from the start; on `context.DeadlineExceeded`, register the still-running process with the `BackgroundProcessManager` (via `Adopt`) and return `formatBackgroundPromotionMessage(sessionID, command, partialOutput)` instead of killing it. Override `cmd.Cancel = func() error { return nil }` so the process isn't killed on context cancel; `Adopt` takes ownership via `os.Process.Pid`. Acceptance: `sleep 150` (2.5 min) is adopted at the 2-min deadline, `check_background` returns accumulated output, `stop_background` kills it; existing fast commands still return synchronously.
- [ ] SP-062-3: **Phase 3 — Wire into agent dispatch.** Add `backgroundProcessManager *tools.BackgroundProcessManager` to the `Agent` struct (next to `terminalManager`); initialize on agent creation when `terminalManager == nil`. Add `BackgroundProcessManagerFromContext(ctx)` helper in `pkg/agent_tools/terminal.go`. Modify `ExecuteShellCommandBackground` / `CheckBackgroundOutput` / `StopBackgroundSession` in `pkg/agent_tools/shell.go` to dual-path: try `TerminalManager` first (WebUI), fall back to `BackgroundProcessManager` (CLI). Acceptance: `sprout agent --no-web-ui "start a dev server and tell me when it's up"` works end-to-end; `background=true` returns a `session_id`; `check_background` returns accumulated output; `stop_background` terminates; the LLM sees identical response shapes on both backends.
- [ ] SP-062-4: **Phase 4 — Cleanup and lifecycle.** On `Agent.Shutdown()`, call `backgroundProcessManager.StopAll()`. On agent startup, scan `~/.config/sprout/background/` for orphan output files: kill any PIDs still alive (recorded in a sidecar `<sessionID>.pid` file), then delete the files. Run a `defer` in the agent's main loop that stops the BPM on cancellation. Add an integration test that drives `sprout agent "start a dev server and check if it's running"` without WebUI and asserts the dev server reports "running" via polling. Acceptance: `make build-all` passes; agent shutdown stops all background processes; orphaned output files are cleaned on next startup; integration test passes.

## SP-048: CLI Delight — Terminal UX Polish
_Spec: `roadmap/SP-048-cli-delight.md`_

Medium-High priority (~1–2 weeks, 5 phases). Five independently shippable phases. Each one alone is a meaningful UX upgrade; together they close the gap with Aider/Claude Code. None require changes to the LLM tool surface.

- [ ] SP-048-1: **Phase 1 — Spinner + tool execution timeline.** Wire a terminal subscriber in `pkg/console/spinner.go` to existing `PublishToolStart`/`PublishToolEnd` events (`pkg/agent/tool_executor.go:77-80`). Between prompt and first stream chunk, render `⠹ Thinking (2.4s · claude-opus-4-7)` on a single erased line (`\r\033[K`); braille frames at 80ms cadence (`⠋ ⠙ ⠹ ⠸ ⠼ ⠴ ⠦ ⠧ ⠇ ⠏`). On tool events, render `⠴ read_file (path) · 0.3s` → `✓ read_file (path) · 0.4s · 1.2k tokens` → `⠦ shell_command: \`go test ./pkg/console/\` · 1.8s`. Suppress entirely when stdout is not a TTY. Acceptance: a multi-tool turn renders a coherent timeline above the stream; piped output is unchanged.
- [ ] SP-048-2: **Phase 2 — Slash command discoverability.** In `pkg/console/input_core.go`, add Tab completion when the buffer starts with `/` — Tab cycles matching command names with an inline ghost suggestion, repeated Tab cycles further matches, non-Tab keystrokes accept the current suggestion. In `pkg/agent_commands/commands.go`, add "Did you mean" via Levenshtein distance on unknown `/foo` (top 2 with descriptions). Add `/help <command>` showing the registered `Usage:` line. Add short aliases (`/m`→`/models`, `/p`→`/providers`, `/x`→`/exit`, `/?`→`/help`) documented in `/help`. Acceptance: typing `/com` + Tab cycles `/commit`, `/compact`, `/commit_review`; `/cmoit` suggests `/commit`; `/help commit` shows its usage.
- [ ] SP-048-3: **Phase 3 — Persistent status footer.** In `pkg/console/footer.go`, render a pinned line at the bottom of the terminal: `─ claude-opus-4-7 · 14.2k/200k ctx · $0.18 · ~/dev/sprout (main) ─────────`. Use terminal scrolling region (`\033[1;<rows-1>r`); update on every `PublishToolEnd` and after each assistant turn. Cost colored yellow >$1, red >$5 (configurable in `pkg/configuration/`). Handle SIGWINCH for resize; restore scroll region on exit (`\033[r`). Suppress when not a TTY. Acceptance: a long multi-tool turn keeps the footer in sync; resizing the terminal redraws correctly; clean exit restores the scroll region.
- [ ] SP-048-4: **Phase 4 — Input ergonomics.** Honor `NO_COLOR` and stop forcing `FORCE_COLOR=1` in `pkg/agent/agent_exec_utils.go:50` unless user explicitly opted in (also update `NewMarkdownFormatter` in `pkg/console/markdown_formatter.go`). Bold the capitalized default letter in `[y/N]` prompts and the safe default option in 4-choice secret prompts (`pkg/ui/prompt.go`, `pkg/agent/secret_prompter.go`). Add smart paste in `pkg/console/input_core.go`: bracketed paste >100 lines or >5KB → confirmation modal `[Use] [Save as file & reference] [Cancel]`; "Save" path drops to `./.sprout/pastes/paste_<ts>.txt` and inserts `@.sprout/pastes/...` (mirror `pkg/console/image_paste.go`). Add Ctrl-R reverse history search (incremental substring, Ctrl-R cycles older, Enter submits, Esc cancels). Add `$EDITOR` escape (Ctrl-X Ctrl-E or `/edit`) opening `$EDITOR` with current buffer pre-filled. Acceptance: `NO_COLOR=1 sprout agent --pipe < script.txt > out.txt` produces clean output; Ctrl-R finds prior prompts; pasting 200 lines offers the save-as-file path.
- [ ] SP-048-5: **Phase 5 — Onboarding + per-turn polish.** On startup in `cmd/agent_modes.go`, if there are sessions from the last 7 days, show up to 3 with their first user message, turn count, and cost; `1`/`2`/`3` resumes them. First-run only (`~/.config/sprout/state.json`): print `Press Tab for slash commands, Ctrl-D to exit, or just start typing.` After each assistant turn in `pkg/agent/agent_exec_utils.go`, print a single dim line: `⎯ this turn: 1.2k in / 4.8k out · $0.04 · 6.1s ⎯`. Change prompt prefix from `sprout> ` to `claude-opus-4-7 ▸ ` (configurable format string). Strip ANSI on non-TTY stdout. Acceptance: first-run shows the hint, subsequent runs do not; turn cost lines appear after each turn; `script -q -c 'sprout agent' out.txt` captures clean output.

## SP-046: Browser-Primary Workspace Sync Model (paid-tier foundation)
_Spec: `roadmap/SP-046-workspace-sync-model.md`_

High priority — explicitly blocks paid-tier UX work. The browser (OPFS) holds the primary authority for user edits; the container holds primary authority for agent edits; both converge via a per-file patch + sequence-number protocol. Note: this is a sprout-side subset of a cross-repo effort (the platform-side wiring lives in `../sprout-foundry`); these items cover the sprout core only.

- [x] SP-072-1: Create `pkg/agent/edit_approval.go` — `EditProposal`/`EditDecision`, a unified-diff hunk splitter, partial apply (original + accepted hunks only), and a broker call reusing the SP-049/SP-068 approval delivery (timeout + webui↔CLI fallback). The tool result states exactly which hunks applied/rejected. Add `edit_approval_test.go`. Acceptance: hunk split + partial apply + reject-all are covered by tests; `go test ./pkg/agent/...` passes.
- [x] SP-072-2: Add `EditApprovalConfig` (`mode: off|all|paths`, `paths` globs) to `pkg/configuration/config.go`; route `write_file`/`edit_file`/`patch_structured_file` in `pkg/agent/tool_handlers_file.go` through approval when active; treat non-interactive runs (`--skip-prompt`/automate/daemon) as approve-all (no hangs). Acceptance: `mode: off` (default) preserves current behavior; `mode: all` gates every write; `mode: paths` gates only matching globs.
- [x] SP-072-3: CLI review UI in `pkg/console` — colored unified diff with a per-hunk `[a]ccept / [r]eject / [s]elect / [e]dit ($EDITOR)` prompt (built on the SP-057 prompt/select primitives). Acceptance: a gated write renders the diff and applies only accepted hunks; rejected hunks never touch the working tree.
- [x] SP-072-4: WebUI — `webui/src/components/EditApprovalPanel.tsx` (per-hunk Accept/Reject, Apply selected / Reject all), `POST /api/edits/{id}/decision`, wired into the tool-event stream and triggering an `input_required` notification (SP-070). Acceptance: `make build-all` passes; a pending edit blocks visibly and applies only accepted hunks.

## SP-073: Cooperative Cancellation — Stop Actually Aborts
_Spec: `roadmap/SP-073-cooperative-cancellation.md`_

Tech debt / UX (Medium-High): ~8 pipelines pass `context.Background()` to the provider, so Ctrl+C / Stop can't abort them (10 `TODO(SP-034-1c)` markers). `a.interruptCtx` is the ready cancellation source.

- [x] SP-073-1: Phase 1 — add a leading `ctx context.Context` param to the leaf functions in `pkg/spec/extractor.go` + `validator.go`, `pkg/codereview/prompts.go`, `pkg/agent_tools/vision_pdf.go` + `vision_analyze.go`, `pkg/agent_commands/commit_review.go` + `shell.go`, and forward it to `SendChatRequest`/`SendVisionRequest` instead of `context.Background()`. Acceptance: `make build-all` passes; those calls use the passed ctx.
- [x] SP-073-2: Phase 2 — callers pass a real cancellation context (tool `Execute(ctx,…)` for handler-driven paths; `a.interruptCtx` for agent methods, incl. the `GenerateResponse` signature note at `agent_getters.go:633`). Acceptance: `grep -rn "context.Background()" ` in the affected pipelines' provider calls returns nothing; `make build-all` passes.
- [x] SP-073-3: Phase 3 — verify Ctrl+C / Stop aborts a multi-page PDF OCR and a commit review within ~1s (clean "aborted" result, no crash); delete all 10 `TODO(SP-034-1c)` markers. Acceptance: `grep -rn "TODO(SP-034-1c)" pkg/` is empty; manual abort confirmed.

## SP-074: Finish the Tool-Registry Migration (retire SP-038 shim)
_Spec: `roadmap/SP-074-tool-registry-migration.md`_

Tech debt (Medium): the dual registry has 3 `TODO(SP-038)` leftovers — `ForPersona` is a stub returning all tools, `ToolEnv.OutputWriter` is hardcoded to `os.Stdout`, and `ToolEnv.ApprovalManager` is `nil`.

- [x] SP-074-1: Implement real per-persona filtering — `(*ToolRegistry).ForPersona(allowlist []string) map[string]ToolHandler` in `pkg/agent_tools/registry.go` (empty allowlist → all tools), with tests for allowlist / empty / unknown-tool. Acceptance: `ForPersona` returns exactly the allowlisted tools; `go test ./pkg/agent_tools/...` passes.
- [x] SP-074-2: Route tool output through the agent — add an `io.Writer` accessor backed by `PrintLine`/`PrintLineAsync` (`OutputRouter`) and set `env.OutputWriter` to it in `pkg/agent/tool_security.go` (keep `os.Stdout` only as the nil-agent fallback). Acceptance: a streaming new-interface tool's output appears in the WebUI, not just stdout.
- [x] SP-074-3: Add a `tools.ApprovalManager` adapter over the agent's `security.ApprovalManager` (signature translation) and set `env.ApprovalManager`. Acceptance: a migrated tool can request approval via the env and it surfaces through the normal CLI/WebUI prompt.
- [x] SP-074-4: Collapse the dual-dispatch fallback for migrated tools (keep only for documented legacy func-style handlers); remove the `TODO(SP-038)` markers. Acceptance: `grep -rn "TODO(SP-038)" pkg/` is empty; existing persona tool-gating is regression-tested unchanged; `make build-all` + `go test ./...` green.

## SP-075: Large-File Decomposition
_Spec: `roadmap/SP-075-large-file-decomposition.md`_

Maintainability (Low-Medium): 20+ files exceed 800 lines (config.go 2833, agent_modes.go 2344, …) vs the 500-line target. Pure, incremental file-level extraction — build + tests green after each step.

- [x] SP-075-1: Phase 1 (config + cmd) — split `pkg/configuration/config.go` (per-domain `config_*.go` for the nested structs + `Resolve()`s), `cmd/agent_modes.go` (per-mode files), `cmd/agent_workflow.go`. Acceptance: each targeted file < ~600 lines or documented exception; `make build-all` + `go test ./...` green; no API diff beyond moves.
- [x] SP-075-2: Phase 2 (agent core) — `pkg/agent/tool_handlers_subagent.go`, `seed_integration.go`, `subagent_runner.go`, `change_tracking_shell.go`. Acceptance: as above, per file.
- [x] SP-075-3: Phase 3 (providers + web) — `pkg/agent_providers/generic_provider.go` ✅ (split into 5 files), `pkg/webcontent/browser_rod.go` (split into 4 files, 1398→max 587), `pkg/wasmshell/commands.go` (split into 4 files, 1633→368), `pkg/console/input_core.go` (1264→706, extracted editing/escape-parser/search), `webui/src/components/Terminal.tsx` (1320→700, extracted `useTerminalPanes` hook). Acceptance: all packages build+test green; no file >750 lines.

## SP-045: WASM Build Feature Parity — distribution tail
_Spec: `roadmap/SP-045-wasm-feature-parity.md` §4–§5 (Tiers 1/2a/2b already shipped)_

- [x] SP-045-1: Strip the WASM binary — add `-ldflags="-s -w"` in `scripts/build-wasm.sh`; confirm the size drop. Acceptance: WASM binary is measurably smaller; still loads.
- [x] SP-045-2: Spike `tinygo` for `cmd/wasm` — assess stdlib/`pkg/agent` compatibility; document go/no-go. Acceptance: written go/no-go with the blocking incompatibilities (if any).
- [x] SP-045-3: Split into a small shell-only WASM module + a lazy-loaded `embedding.wasm` (loads on first semantic search). Acceptance: casual page load no longer pulls the embedding module.
- [x] SP-045-4: Build-matrix hygiene — tag `pkg/webui/terminal_*.go` (creack/pty importers) `!js` and sweep `//go:build !windows` that wrongly include `js`. Acceptance: `GOOS=js GOARCH=wasm go build ./...` is clean.

## SP-015: Cloud Platform Integration — remaining follow-ups
_Spec: `roadmap/SP-015-cloud-platform.md` (R1/R3/R5 already complete)_

- [x] SP-015-R4: Add a CI check that fails if Foundry's Service-Worker route table diverges from `dist/endpoint-manifest.json`; evaluate a shared `@sprout/endpoint-registry` package. Acceptance: a manifest/route-table divergence fails CI.
- [x] SP-015-R6: Define the canonical dist-bundle layout the browser IDE expects (`index.html`, `assets/`, `wasm/`) and make `scripts/build-webui-dist.mjs` produce exactly that. Acceptance: the build output matches the documented layout.
- [x] SP-015-R7: Chat-translation robustness — edge-case tests (empty query, missing `chat_id`, steer, stop), document the Foundry chat contract, and consider extracting the `{query}`→`{messages,stream}` translation into a module shared with Foundry's `chat-bridge.ts`. Acceptance: edge-case tests pass; the contract is documented.

## SP-078: Steer-Panel UX Parity — Wrap-Aware Rendering, Tab Completion
_Spec: `roadmap/SP-078-steer-panel-ux-parity.md`_

UX parity (Medium): the pinned steer-input panel (`pkg/console/steer_input.go`, 1428 LOC) lacks the wrap-aware render path and Tab completion that the regular `InputReader` has across `pkg/console/input_*.go` (~6300 LOC across 18 files). User-visible: long single-line steers overflow horizontally, the caret lands off-column on wrapped multi-line steers, and there's no slash-command completion mid-turn (Tab is reserved for STEER↔QUEUE mode toggle). All five prior steer fixes (`e830d113`, `8f501bd3`, `6714f690`, `eb441143`, `ac75f0ed`) remain green.

- [x] SP-078-1: Phase 1 — wrap-aware render path. Extract `wrappedGeometry`/`writeWithHardBreaks`/`visibleRuneWidth`/`cursorLineIndex`/`cursorColumnOffset` from `input_render.go` into `pkg/console/wrap.go`; in `steer_input.go` run the buffer through `writeWithHardBreaks(buffer, cols-promptWidth)` and forward `(lineIdx, colOnLine)` to the footer; extend `StatusFooter.SetSteerLineWithCursor` (or add a sibling) to accept a pre-split `lines []string` + per-line cursor placement so the caret lands at the right visual cell after soft wraps. Clamp the visible cap (keep `maxSteerRows=6`, scroll via leading `…` for longer wrapped content). Acceptance: a 200-char steer in an 80-col terminal wraps to ≤6 rows with caret at the correct (line, col) for every cursor position; `pkg/console/steer_input_wrap_test.go` covers narrow/wide terminals, ASCII/CJK, and mid-wrap cursor positions; `go test ./pkg/console/...` green.
- [x] SP-078-2: Phase 2 — Tab completion on a non-conflicting binding. Move `CompletionProvider` + `completionCycle` + `handleTabCompletion` from `input_completion.go` into `pkg/console/completion.go`; add `(*SteerInputReader).SetCompleter(c CompletionProvider)`; wire the slash-command registry as the steer completer. Binding: `Ctrl+]` (poll in TODO before implementation; alternatives: `Shift+Tab`, `Alt+/`). Acceptance: typing `/mo` + the new binding cycles `/model`, `/mode`, etc. mid-turn; empty candidate set is silent; existing `Tab` mode-toggle behavior unchanged; `TestSteerInput_Completion_*` family green.
- [x] SP-078-3: Phase 3 — soft-wrap cursor position fix in `status_footer.go:540`. Replace the naive `cursorByteCol = steerCursor - offset` with `visibleRuneWidth(lineText[:byteCol])` so the caret lands at the correct visual column when a `\n`-separated line is wider than the terminal and the user has wrapped within it. Acceptance: `TestStatusFooter_SteerPanel_Wrap` covers narrow terminal + over-wide single line + cursor at the wrap boundary; existing status-footer tests green.
- [ ] SP-078-4: Phase 4 — close-out. `grep -rn "TODO(SP-078)" pkg/console/` is empty; `make build-all` + `go test ./...` green; add a recording-style screenshot or `browse_url` snapshot of a wrapped steer so future regressions surface at review time.
- [ ] SP-046-1: **Sync protocol + per-file metadata.** Add `pkg/agent_tools/workspace_sync.go` defining `FileMetadata { browser_seq, container_seq, last_synced_browser, last_synced_container, modified_at }` and the patch event payload `{ path, container_seq, content, base_browser_seq }`. Define the WS envelope (`workspace.patch_in`, `workspace.patch_out`, `workspace.heartbeat`) and the HTTP `POST /api/workspace/op` outbound endpoint in `pkg/webui/workspace_sync_handlers.go`. Acceptance: a Go test drives the protocol end-to-end (container emits patch → browser acks with `last_synced_container`; browser emits op → container applies + bumps `container_seq`); the per-file sequence rules from spec §3 are honored.
- [ ] SP-046-2: **Agent staleness rule (spec §7).** Add a wrapper in `pkg/agent_tools/write_file.go` that refuses `write_file(path, …)` when the file's `browser_seq > last_read_seq` OR `last-modified < 30s ago` OR the agent hasn't called `read_file(path)` this turn, returning the exact error string from the spec. Hook into `tool_handlers_file.go`. Add `write_file_staleness_test.go` covering: stale-by-sequence, stale-by-recent-mtime, stale-by-no-read-this-turn, and the happy path. Acceptance: a write with a stale read is refused with the spec's error message; regression test asserts the wrapper is invoked for `write_file`/`edit_file`/`patch_structured_file` tool handlers.
- [ ] SP-046-3: **Conflict detection + sibling-file surfacing.** When a container patch arrives for a file with `browser_seq > last_synced_browser`, write the container's version as `<path>.theirs` and emit a `workspace.conflict_detected` event with both versions' hashes; do not overwrite the browser file. Add `workspace_conflict_test.go` covering the divergence detection (single-side edit, simultaneous edit, no-conflict baseline). Acceptance: simultaneous edits produce a `.theirs` sibling and an event; single-side edits apply cleanly; `make build-all` passes.
- [ ] SP-046-4: **Long-running-job heartbeat (spec §4).** Browser sends `workspace.heartbeat` every 15s; container monitors and reaps sessions with >60s missed heartbeats (treats as abandoned, terminates the running job). Emit `workspace.heartbeat_lost` before termination for graceful UI surfacing. Add `workspace_heartbeat_test.go` covering heartbeat up, missed heartbeat (terminate after 60s), and transient blip (≤ 60s tolerated). Acceptance: stopping the heartbeat stream causes the session to reap within 60s + grace; the WebUI receives the `heartbeat_lost` event.
- [ ] SP-046-5: **Multi-device single-active-session (spec §5).** On second-device connection, surface a takeover prompt; first device's WS is closed with a `workspace.session_moved` event. Implement in `pkg/webui/workspace_sync_handlers.go` + frontend `webui/src/services/workspaceSync.ts`. Add `multi_device_takeover_test.go`. Acceptance: opening sprout on a second device produces the takeover prompt on both; accepting closes the first device's WS with the moved event.
