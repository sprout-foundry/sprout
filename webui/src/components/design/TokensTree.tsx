/**
 * Tokens tab body — placeholder for SP-140-3 item 3.8.
 *
 * The real tree (grouped DTCG nodes, color swatches, typography/spacing
 * specimens, path search) lands in item 3.8; this stub keeps the shell's tab
 * structure complete and type-checked.
 */

import type { DesignTabProps } from './DesignTabProps';

export default function TokensTree(_props: DesignTabProps = {}) {
  return (
    <div className="design-tab-body" data-testid="design-tokens-tree">
      <p className="design-tab-placeholder">Token tree — not yet implemented (SP-140-3 item 3.8).</p>
    </div>
  );
}
