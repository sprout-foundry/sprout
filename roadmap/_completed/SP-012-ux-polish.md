# SP-012: UX Polish

**Status:** ✅ Implemented (a11y gap-closure landed 2026-07-01 — `role="treeitem"`/`aria-expanded` on FileTree rows, `aria-live="polite"` on ChatPanel log region, global `:focus-visible` styles in `webui/src/index.css`, `notificationBus.markAllRead()` + NotificationCenter "Mark all read" button; broader SP-012 surface already shipped 2026-06-23 → 2026-06-30 per the spec history)

Full specification archived. See git history for original content.