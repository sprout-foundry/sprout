Analyze codebase workspace overview and return JSON with technical attributes:

Run `pwd` first, then analyze.

Required JSON fields:
- "primary_frameworks": Major frameworks/libraries (concise)
- "key_dependencies": Notable third-party deps (concise)  
- "build_system": Build process (concise)
- "test_strategy": Testing approach (concise)
- "architecture": Overall architecture (brief)
- "monorepo": "yes"/"no"/"unknown"
- "ci_providers": CI providers/configs present
- "runtime_targets": Runtime environments
- "deployment_targets": Deployment targets
- "package_managers": Package management tools
- "repo_layout": Directory structure pattern

JSON only - no other text.