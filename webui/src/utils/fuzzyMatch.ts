/**
 * Fuzzy matching utilities.
 *
 * Re-exported from @sprout/ui — the shared canonical source.
 * All fuzzy matching logic is defined in packages/ui.
 */

export { fuzzyScore, fuzzyFilter, highlightMatches, type FuzzyResult } from '@sprout/ui';
