# TODO

Active work tracked here. Completed items are removed once their parent spec is moved to ✅ Implemented in `roadmap/README.md` — the spec file itself is the historical record.

## SP-053: WebUI CLI Parity — Persona/Depth, Live Tools, Cost Footer
_Spec: `roadmap/SP-053-webui-cli-parity.md`_

Closes the visible feature gap between the polished CLI and the still-flat WebUI. The CLI got three rounds of polish (SP-048 spinner+footer+timeline, SP-050 persona collapse, SP-051 depth-aware subagent UI). The WebUI didn't move during any of those. Each phase is independently shippable.

### Phase 1: Persona + depth on chat messages
- [ ] SP-053-1a: Hoist `PERSONA_COLORS` from `webui/src/components/chat/SubagentActivityFeed.tsx:11-24` into the shared `@sprout/ui` package (e.g. `packages/ui/src/persona.ts`) so it can be reused by `MessageBubble`, `StatusBar`, and `ToolCard`. Keep the existing local map working (re-export from the shared location) so nothing breaks. Add `orchestrator` to the map (currently absent — it was collapsed in SP-050). Each entry is `personaID -> { color: string, label: string }`. Acceptance: `webui/src/components/chat/SubagentActivityFeed.tsx` imports from `@sprout/ui` instead of defining inline; the shared module exports a `personaColor(id)` and `personaLabel(id)` helper; `orchestrator` resolves to a distinct color.
- [ ] SP-053-1b: Extend the shared `Message` type (`packages/ui/src/types/chat.ts:20-27`) with optional `persona?: string` and `subagentDepth?: number` fields. These are populated by the WebUI's event handler from the `active_persona` / `subagent_depth` event-metadata keys that SP-051 already puts on every published event (`pkg/agent/agent_events.go:19-53` `decorateEventPayload`). The fields are optional and backwards-compatible (existing messages without them render unchanged). Acceptance: the type change compiles across `packages/ui` and `webui`; a `Message` with `persona: "coder", subagentDepth: 2` type-checks.
- [ ] SP-053-1c: Surface persona/depth on `MessageBubble` (`packages/ui/src/components/MessageBubble.tsx`). When `message.persona` is present and non-empty, render a colored badge (using the hoisted `personaColor`) before/above the bubble content. When `message.subagentDepth > 0`, indent the bubble by `subagentDepth * N px` (N≈12-16). Depth 0 = no indent, no badge (the primary session). Respect `NO_COLOR`-equivalent for the WebUI (CSS class fallback). Acceptance: a depth-2 coder message shows a cyan "coder" badge and is indented two levels; a depth-0 message is unchanged from today; the `MessageBubble` snapshot/visual test (if one exists) still passes for depth-0, and a new snapshot captures the badged+indented case.
- [ ] SP-053-1d: Wire the WebUI event handler to populate the new fields. In the code that translates incoming agent events into `Message`/`ToolExecution` objects (find the SSE/WebSocket event reducer — likely in `webui/src/` near the chat store), read `active_persona` and `subagent_depth` off each `AgentMessage`/`StreamChunk`/`ToolStart` event payload and stamp them onto the message. Acceptance: a real subagent run in the WebUI shows the badge + indent from 1c.

### Phase 2: Live tool feedback (spinner per tool)
- [ ] SP-053-2a: Add a "running" state render to `ToolCard` (`webui/src/components/contextPanel/ToolCard.tsx`). Today `ToolCard` only renders completed tools. When a `ToolExecution` has `status === 'running'`, render it with a spinner (reuse the existing `@sprout/ui` spinner component) and a "(running…)" label, plus the tool name + args preview. When the matching `ToolEnd` arrives, flip to the completed render path. Acceptance: a `shell_command` that sleeps 2s shows a spinner in the tools panel for ~2s, then flips to the result; previously the panel stayed empty until completion.
- [ ] SP-053-2b: Replace the generic `Processing...` skeleton in `ChatFooter.tsx:54-63` with a live tool indicator. While any tool is in `status === 'running'`, show `Running: <toolName> (<preview>)` in the footer, updated as tools start/end. When no tool is running but the agent is still generating, fall back to the existing typing/thinking indicator. Acceptance: during a multi-tool turn the footer cycles through each tool's name as it runs, rather than a static "Processing...".

### Phase 3: Cost / model / context in the status bar
- [ ] SP-053-3a: Surface `ChatProps.stats` in `StatusBar.tsx:50-122`. The cost/model/token data is already plumbed through `ChatProps.stats` (`packages/ui/src/types/chat.ts:153`) but not rendered. Add a right-aligned cluster showing `model · k/NNNk ctx · $cost` using the shared `StatusBar` `rightItems` slot (already supported — no schema change). Format tokens with the existing `formatThousands` helper from the CLI for consistency. Acceptance: the status bar shows `claude-haiku-4-5 · 14.2k/200.0k ctx · $0.42` on each refresh; when stats are absent/zero the cluster is hidden (no flicker on first load).
- [ ] SP-053-3b: Update `StatusBar` snapshot tests (in `packages/ui`) to cover the new rightItems cluster. Add a case with stats present (assert model + cost + ctx render) and a case with stats absent (assert the cluster is omitted). Acceptance: new snapshot tests pass; existing StatusBar snapshots unchanged for the no-stats case.

### Phase 4: Build + verification
- [ ] SP-053-4a: Run `make build-all` (builds Go binary + React UI + WASM) and `go test ./...`. Verify the React UI builds cleanly with the new shared `@sprout/ui` exports and type changes. Confirm no regressions in the Go test suite. Acceptance: `make build-all` exits 0; `go test ./...` has no new failures vs. baseline (known environmental failures in `pkg/console` TTY tests and `pkg/agent` ONNX model tests are pre-existing and acceptable).
