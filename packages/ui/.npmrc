# @sprout/ui npm publishing configuration
# Access level: public (scoped package)
access=public

# Dry run by default for safety — use `npm publish --no-dry-run` to actually publish
dry-run=true

# Ensure the package is published with the correct registry
registry=https://registry.npmjs.org/
