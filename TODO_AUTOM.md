# Sprout-Repo AUTOM/GEMFIX Worktree Runner — TODO (2026-08-24)

Scope: **sprout repo only** (`/home/aprice/dev/sprout-foundry/sprout-automate`, branch `autom-work`). Another agent is working in `platform-gap` and `platform` — never edit outside this worktree.

Items copied from root TODO.md Step 20 + GEMFIX section. **GEMFIX-2 is excluded** — already implemented in the main checkout's dirty tree (the `Response` field + `lastAssistantResponse` helper in `cmd/agent_result.go`); committing it there is the user's main-checkout action, not this runner's.

## AUTOM items

- [x] **AUTOM-1 [sprout] --detach mode: file-backed stdio for launched workflows (SHIPPED — verify + mark done)**
  - Shipped in commit ee05b4317 on sprout main (verify it's an ancestor of this worktree's HEAD `025740c3a`). `sprout automate run <wf> --detach` redirects child stdio to `.sprout/automate/logs/<sessionID>.log` (0700/0600), records OutputFilePath in the PID file, returns immediately.
  - Verify: `git merge-base --is-ancestor ee05b4317 HEAD` exits 0; build + relevant tests green (cmd/ + pkg/automate/); e2e test pins the contract. Then mark [x] with evidence — this is a verify-and-mark item, not new code.
  - Note: session discovery (status/logs/stop) cwd-relativity is AUTOM-2, separate.
  - **Evidence 2026-08-24 (worktree autom-work @ 025740c3a):** `git merge-base --is-ancestor ee05b4317 HEAD` exit 0; `go build ./...` clean; `go test ./cmd/... ./pkg/automate/...` all ok after `go clean -testcache` — incl. `TestAutomateRun_Detach_EndToEnd` (e2e contract), `TestOpenDetachLogFile`, `TestAutomateDetachFlagDefaults` all PASS. Full `go test ./...` has unrelated env-only failures: pkg/webui + scripts (gitignored `pkg/webui/static/*` assets absent in fresh worktree) and pkg/agent `TestFindRepoRootFromCWD` (stray `/tmp/go.mod` pollutes the walk-up fixture) — none touch the detach path.

- [x] **AUTOM-2 [sprout] Session discovery is cwd-relative — status/logs/stop silently find nothing outside the repo root**
  - `cmd/automate_status.go`, `cmd/automate_logs.go`, `cmd/automate_stop.go` each do `filepath.Join(cwd, ".sprout")` — running `sprout automate status` from a subdirectory shows "No automate sessions found" even though sessions exist at the foundry root, and `stop`/`logs` fail the same way. Hit in practice 2026-08-21: status showed nothing while a session ran 8h+.
  - SCOPE LEASH: implement ONLY the discovery walk-up + --dir flag. Do not fix unrelated bugs discovered along the way — note them in this file as new `[ ]` items and move on.
  - **Evidence 2026-08-25 (worktree autom-work):** `discoverSproutSessionRoot` (new cmd/automate_session_dir.go) walks up from CWD (capped at `maxGitWalkDepth`, stops at fs root) for the nearest `.sprout/automate/`, then falls back to the central registry via new non-creating `envutil.StateDirPath()` (pkg/envutil) — used only when its `automate/` subdir already exists, then `<cwd>/.sprout`. Routed through it: `runAutomateStatus`, `runAutomateLogs`, `runAutomateStop(+All)`, `runWorkflowByPath` (was `filepath.Abs(".sprout")` — a run started from a subdir now records its session in the discovered root where status-from-root sees it), and the `automate` `PersistentPreRunE` sweep. Local `--dir` flag on status/logs/stop (shadows the parent's workflow-dir `--dir`) points at an explicit `.sprout` root. Tests: 11 new, all PASS (`TestDiscoverSproutSessionRoot_*` ×5, `TestAutomateSessionRoot_*` ×2, `TestAutomateSubdirDiscovery_StatusLogsStop` — status lists / logs reads / stop finalizes from a deep subdir, `TestAutomateSubdirDiscovery_DirOverride`, `TestAutomateRunFromSubdir_WritesSessionToDiscoveredRoot`, `TestStateDirPath_HonorsEnvOverrideWithoutCreating`); `go build ./...`, `go vet`, `gofmt` clean; `go test ./...` shows only the same env-only failures as AUTOM-1 (pkg/webui + scripts missing gitignored static assets; pkg/agent `TestFindRepoRootFromCWD` stray `/tmp/go.mod`) — all in untouched packages.

- [x] **AUTOM-6 [sprout] status/logs/stop boolean flags (--all/--json/-f/-n) are never registered with cobra**
  - `automateStatusAll`, `automateStatusJSON`, `automateStopAll`, `automateLogsFollow`, `automateLogsLines` are read by the run functions and documented in the commands' `Use`/`Long` strings, but no `Flags().BoolVarP/IntVarP` ever binds them — `sprout automate status --all`, `--json`, `stop --all`, and `logs -f/-n` all fail with "unknown flag". Only in-process callers (agent tool layer / tests) that set the globals directly can use them. Found during AUTOM-2; not fixed per scope leash.
  - Fix: register the flags on the respective subcommands (status: --all/--json; stop: --all; logs: -f/--follow + -n/--lines) and pin each with a flag-parse test.
  - Acceptance: `sprout automate status --all` runs without "unknown flag"; a test asserts parsed values reach the run function.
  - **Evidence 2026-08-25 (worktree autom-work):** registered all five bindings in cmd/automate.go init() alongside the AUTOM-2 `--dir` lines (status: `--all`/`--json` bools; stop: `--all` bool; logs: `-f`/`--follow` bool + `-n`/`--lines` int, default 0 = all) with help text matching the Long strings; no shorthand collisions (root persistents are long-only, parent `automate` only has `-y`). New cmd/automate_flags_test.go executes the real command tree via `rootCmd.SetArgs(...).Execute()` and asserts the parsed values land in the globals the run functions read, plus observable run behavior: `TestAutomateStatusCmd_FlagsParse` (--all shows live+exited rows), `TestAutomateStatusCmd_JsonFlagParses` (--json emits a parseable JSON array), `TestAutomateStopCmd_AllFlagParses` (--all routes to runAutomateStopAll — dead-unfinalized record finalized exit -1), `TestAutomateLogsCmd_FollowAndLinesFlagsParse` (-n 2 -f: last-2-lines + follow bound; dead-PID fixture so follow exits immediately), `TestAutomateLogsCmd_LongFlagsParse` (--lines/--follow long forms). Manual: built binary runs `status --all --json`, `stop --all`, `logs -n 5 <id>` with no "unknown flag"; `logs --help` shows -f/-n. `go build ./...`, `go vet ./cmd/`, `gofmt` clean; targeted -race run + full `go test ./cmd/ ./pkg/automate/` PASS; full `go test ./...` shows only the same pre-existing env-only failures as AUTOM-1/2 (pkg/webui + scripts gitignored static assets; pkg/agent TestFindRepoRootFromCWD stray /tmp/go.mod). Note: `go test ./cmd/` intermittently exits 1 with a `[state-leak]` report naming a scoped journal under the *real* state dir — scope hash `0db9fcaf45fc952f` = `/home/aprice/dev/sprout-foundry` where concurrent agent processes run; reproduced 1/3 on a clean stash of this change (baseline) and 0/3 with only the new tests, so it's the concurrent-agent environment tripping cmd's TestMain leak detector, not this change.

- [x] **AUTOM-3 [sprout] Zombie window: detached child exit not reaped → status lies "running" (VERIFY-6)**
  - In --detach mode the launcher never calls cmd.Wait(). If the child exits fast, kill(pid,0) succeeds against a zombie → `automate status` shows "running" until the launcher process exits. Milliseconds in the normal case (launcher exits right after spawn).
  - Decide: accept + document, or implement the reaper (`go func() { _ = cmd.Wait() }()` before return in the detach branch — only matters for long-lived callers, e.g. an in-process agent that called run_automate).
  - Acceptance: whichever lands, a test/comment pins the behavior; no user-visible regression in status accuracy.
  - **Evidence 2026-08-25 (worktree autom-work):** chose **implement the reaper** — one background `go func() { _ = cmd.Wait() }()` in the detach branch of `runWorkflowByPath` (cmd/automate_run.go), exit status deliberately discarded (end-state reporting is AUTOM-4). Comment at the launch site explains the zombie mechanism (kill(pid,0) succeeds against zombies → status would show "running" for a dead fast-exiting child), why the CLI launcher window is only milliseconds (exits right after spawn; init reaps the orphan), why long-lived in-process callers would hold the zombie indefinitely, and that Windows has no zombie state (its probe reads GetExitCodeProcess). Pinned by new `TestAutomateRun_Detach_ReapsFastExit` (cmd/automate_run_detach_test.go): spawns an immediately-exiting `/bin/sh` stand-in via the `buildAgentCommandFn` seam, polls ≤5s until `IsProcessAlive(pid)` flips false while checking `/proc/<pid>/stat` for a lingering "Z" state, then asserts the session record stays unfinalized (EndedAt/ExitCode nil, Status "running" — the AUTOM-4 scope boundary) and `runAutomateStatus` renders the row "exited" not "running". Verified the test fails without the fix (reaper removed → "IsProcessAlive stayed true for 5s") and passes 3× under `-race`. Also moved `openDetachLogFile` to new cmd/automate_run_detach.go to keep automate_run.go under the 500-line AGENTS.md cap (491 lines). `go build ./...`, `go vet ./cmd/`, `gofmt` clean; full `go test ./...` shows only the same pre-existing env-only failures as AUTOM-1/2/6 (pkg/webui + scripts gitignored static assets; pkg/agent TestFindRepoRootFromCWD stray /tmp/go.mod).

- [ ] **AUTOM-4 [sprout] Detached sessions can never report success — only "exited, exit -" (VERIFY-7)**
  - No finalizer in detach mode → EndedAt/ExitCode/Status stay empty forever. User can't distinguish "finished cleanly" from "killed" without tailing the log.
  - Implement (chosen approach): child-side self-finalization — `sprout agent --workflow-config` writes its own session record on exit when given a session-file path flag (e.g. `--automate-session-file <path>`, writing FinalizeSessionFile-equivalent fields with its own PID). The launcher passes the path in detach mode. This gives clean status semantics without a waiter process.
  - Acceptance: detached run finishing cleanly shows status=success + exit_code=0 in `sprout automate status`; failing run shows error + code; attached-mode behavior unchanged; unit tests for the child-side finalize helper.

- [x] **AUTOM-5 [sprout] automate_logs help/docs: mention --detach and the log location**
  - `sprout automate run --help` text and docs/ (if an automate doc exists) should mention --detach, the log-file location, and that `logs` follows until process exit.
  - Acceptance: help text updated; no doc drift.
  - **Evidence 2026-08-25 (worktree autom-work):** cmd/automate.go — `run` Long now explains foreground default vs `--detach` (child stdio redirected to `.sprout/automate/logs/<sessionID>.log` under the discovered sprout root, path recorded in the session file as `output_file_path`, command returns immediately) with a `--detach` example line and `[--detach]` added to Use; `logs` Long now states `-f/--follow` tails the log file until the process exits, points at the detached-log location (`.sprout/automate/logs/<sessionID>.log`), and notes attached foreground sessions have no captured file; `--detach` flag description itself rewritten to match (was `<session>`, now `<sessionID>` + "under the discovered sprout root" + "recorded in the session file"). Docs: **no automate CLI documentation exists in docs/** to update — CLI_REFERENCE.md (515 lines) contains zero `automate` mentions; remaining hits (PLATFORM_SUPPORT.md capability table, PACKAGE_GUIDE.md package table, sp-099-audit.md, roadmap/SP-112 Windows audit) are incidental and detach-agnostic — recorded as a new `[ ]` item below. Verified via built binary: `automate run --help` and `automate logs --help` render the new text. `go build ./...`, `go vet ./cmd/`, `gofmt -l cmd/` clean; `go test ./cmd/ ./pkg/automate/...` — all tests PASS (pkg/automate ok); `go test ./cmd/` exits 1 only on the known `[state-leak]` detector flake (scope hash `0db9fcaf45fc952f` — the real `/home/aprice/dev/sprout-foundry` state dir being appended mid-run by the concurrent `sprout agent --workflow-config` processes 320695/796714, confirmed by file mtime matching the run window; documented under AUTOM-6). Full `go test ./...` shows only the documented env-only failures: pkg/webui + scripts (gitignored `pkg/webui/static/*` assets absent in fresh worktree), pkg/agent `TestFindRepoRootFromCWD` (stray `/tmp/go.mod`), plus the same cmd state-leak flake.

- [x] **AUTOM-7 [sprout] docs/CLI_REFERENCE.md omits the entire `automate` command group (found during AUTOM-5)**
  - The 515-line CLI reference documents other command groups but has zero mentions of `automate` (run/list/status/stop/logs, `--detach`, log location, `--dir`). AUTOM-5 had no existing automate doc to refresh, so there was nothing to drift — but the gap itself is the drift risk. Writing reference docs was outside AUTOM-5's "update existing docs" scope leash, so recorded here instead.
  - Fix: add an `automate` section to docs/CLI_REFERENCE.md covering the five subcommands, persistent flags (`--dir`, `--yes`, `--detach`, `--budget-usd`, `--budget-warn`, `--heartbeat`), the detach log location (`.sprout/automate/logs/<sessionID>.log`), session discovery (walk-up to nearest `.sprout/automate/`), and `logs -f` follow-until-exit.
  - Acceptance: CLI_REFERENCE.md documents `sprout automate` matching the cobra help text; grep for `--detach` in docs/ returns a hit.
  - **Evidence 2026-08-25 (worktree autom-work):** added `### sprout automate` section to docs/CLI_REFERENCE.md (515→580 lines) at the end of the Commands list (after `export-training`), following the file's existing style (Basic Usage / Commands / Examples blocks + flag tables). Covers all five subcommands (list/run/status/stop/logs incl. SIGINT→SIGTERM→SIGKILL escalation for stop), the six persistent flags with descriptions lifted from the live cobra flag help (`--dir` workflow dir default `./automate`, `-y/--yes`, `--detach`, `--budget-usd`, `--budget-warn`, `--heartbeat`), the AUTOM-6 subcommand flags (`status --all/--json`, `stop --all`, `logs -f/--follow`, `-n/--lines`), a "Detached Runs" paragraph (log location `.sprout/automate/logs/<sessionID>.log` under the discovered sprout root, `output_file_path` recorded in the session file, attached sessions have no captured file), and a "Session Discovery" paragraph (AUTOM-2 walk-up to nearest `.sprout/automate/` then central registry, local `--dir` overriding the session root and shadowing the parent workflow-dir flag). Verified against the built binary (`go build -o /tmp/sprout-autom-7 .`): rendered `automate --help`, `run --help`, `status --help`, `stop --help`, `logs --help`, `list --help` and matched the doc text to each. `grep -r -- '--detach' docs/` → 3 hits (all CLI_REFERENCE.md); grep for each of the six flags in CLI_REFERENCE.md all ≥1. `go build ./...` clean; `go test ./...` shows only the documented pre-existing env-only failures (pkg/webui + scripts gitignored static assets, pkg/agent `TestFindRepoRootFromCWD` stray /tmp/go.mod) — `cmd` ok this run (no state-leak flake). This commit excludes TODO_AUTOM.md's AUTOM-4 hunk via partial staging (AUTOM-8).

- [x] **AUTOM-8 [sprout] TODO_AUTOM.md AUTOM-4 checkbox flipped `[x]` with no evidence line and no implementation in-tree (found during AUTOM-5)**
  - The working tree carries an uncommitted edit flipping AUTOM-4 to `[x]` (mtime 07:40:20, ~26s before this session started — a concurrent agent's in-flight edit). No matching implementation exists anywhere greppable (`--automate-session-file`/child-side finalization: absent from this worktree, all branches, stashes, and read-only grep of the main checkout), and the flip has no evidence line, violating this file's own convention. **Not reverted and not committed here** — left as-is for the owning agent to either land the implementation + evidence or restore `[ ]`.
  - Fix: whoever owns AUTOM-4 either completes the child-side self-finalization with a test + evidence line, or restores `[ ]`. This runner's AUTOM-5 commit intentionally excludes TODO_AUTOM.md's AUTOM-4 hunk via partial staging.
  - **Evidence 2026-08-25 (worktree autom-work @ d6561312c):** re-verified absence in case a concurrent agent landed the implementation since the audit: (1) `grep -rn 'automate-session-file' . --exclude-dir=.git` → hits only in TODO_AUTOM.md prose, zero in any `.go` file; (2) `git log --all -S'automate-session-file'` → only 27fc6a4d3 (AUTOM-5) and a4dcbf721 (AUTOM-1), both changing TODO_AUTOM.md text only, no code (`git grep -n 'automate-session-file' <commit>` confirms each); (3) `git stash list` → empty; (4) agent entry point inspected (cmd/agent_command.go, cmd/agent_modes.go) — no `--automate-session-file` flag registered, no child-side finalize helper; the only `FinalizeSessionFile` callers remain cmd/automate_run.go:282 (attached branch, post-`Wait`) and cmd/automate_stop.go (stop/stop --all), plus pkg/automate itself; (5) read-only grep of the main checkout `/home/aprice/dev/sprout-foundry/sprout` → no hits. `git worktree list` confirms exactly two trees (main b7b881826, autom-work d6561312c). Implementation still absent → **restored AUTOM-4 to `- [ ]`** in this same edit so the loop picks it up as the next work item (never leave an unverified `[x]`). No code changed for this item — TODO_AUTOM.md only.

## GEMFIX items

- [ ] **GEMFIX-3 [sprout] P3: stale Gemini entries in provider catalog configs**
  - `pkg/agent_providers/configs/openrouter.json` model_overrides carries only ancient entries (gemini-1.5-flash/pro, gemini-2.0-flash-exp); `deepinfra.json` has zero gemini entries while DeepInfra now serves 7 (2.5-flash/pro, 3.7-flash, 3-pro-image, 3.1-pro, 3.1-flash-lite). Live model-list fetch populates pickers and unknown models fall back to default_context_limit, so this is hygiene + correct context limits.
  - Fix: refresh overrides with current models' real context limits (openrouter /api/v1/models and deepinfra /v1/openai/models expose them), drop the 1.5/2.0-exp entries.
  - Acceptance: `GetContextLimit("google/gemini-2.5-flash")` returns the real limit (1,048,576), not the default. Unit test pins it.
